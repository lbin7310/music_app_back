// Global

const home = "/";

// Album

const album = "/album";
const albumDetail = "/:id";

// Routes

const routes = {
  HOME: home,
  ALBUM: album,
  ALBUM_DETAIL: albumDetail
};

export default routes;
